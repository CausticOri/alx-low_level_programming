#include "main.h"

/**
 * mul  - multiplies two integers
 * @a: the first number to multiple
 * @b: the second number to multiply with the first number
 * Return: the result of the multiplication between a param and b param
 */

int mul(int a, int b)
{
	return (a * b);
}
